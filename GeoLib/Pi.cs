using System;

namespace GeoLib
{
    public static class Pi
    {
        public const float QuarterCircle = (float)Math.PI * 0.5f;
        public const float HalfCircle = (float)Math.PI;
        public const float FullCircle = (float) Math.PI * 2f;
    }
}
